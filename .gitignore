.claude/*
!.claude/skills/

# OS
.DS_Store

# Editor
.vscode/
.idea/

# Python
__pycache__/
*.pyc

# Backups written by make-pdf.sh / update_bibliography.py
*.bak

# Local-only CV variants (see make-pdf.sh --variant / --funding).
# Never published: this repo is public, so keep them out of git entirely.
# NB: teagueCV-*.pdf deliberately does not match the tracked teagueCV.pdf.
variants/
funding.html
teagueCV-*.pdf
.cv-build.html*
.cv-frag.html*
